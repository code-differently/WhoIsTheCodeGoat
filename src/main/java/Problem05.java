public class Problem05 
{
    public boolean checkIfReversed(String array)
    {
        int count = 0;

        char[] chrArray = array.toCharArray();
        char[] chrSorted = chrArray.clone();
    }
}
